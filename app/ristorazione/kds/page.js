import DemoEmbed from '../DemoEmbed';

export const metadata = { title: 'KDS Cucina + Bar | Mente Locale' };

export default function Page() {
  return (
    <DemoEmbed
      kicker="03 · Cucina"
      title="KDS CUCINA + BAR"
      lead="Comanda dal tavolo → cucina + bar in 0.3s. Zero carta."
      src="/ristorazione/kds.html"
    />
  );
}
